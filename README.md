# Studio · Workspace — Creative Companion

Internal iDesign Creative source-of-truth (interim prototype). Access is fronted by the Studio hub; editing is admin-locked. Real backend (auth + Postgres + Asana) is the GCP build.
